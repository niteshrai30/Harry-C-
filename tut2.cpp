#include<iostream>

using namespace std;                         // cout and cin are declared in this library.

int main(void)
{
    cout << "Hello_World " << endl ;                       // in this case we have to use (using namespace std)
                                                          // endl is used for jump to new line

   // std :: cout << "Hello_World \n" ;                // when we are not using (using namespace std) then we have to do this,
    return 0;
}