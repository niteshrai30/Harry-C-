#include<iostream>
using namespace std;

int main(void)
{

     // std ::cout << "Hello World  \n" ;    // when namespace std is not declared.
        cout << "Hello World  \n" ;       
            
        
      //   if we donot declare std in namespace then we have to do scope resolution of std,std::cout,
      //   we have to do either define std in namespace or define scope resolution of std,

      return 0;    // successful termination of the function.
}